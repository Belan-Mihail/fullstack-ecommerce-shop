import React from 'react'
import Button from 'react-bootstrap/Button';

function Header() {
  return (
    <div>
        <Button  variant="warning">Header</Button>
    </div>
  )
}

export default Header